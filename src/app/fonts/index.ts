import { Inter, Geist_Mono } from 'next/font/google'

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
  variable: "--font-inter",
})

export const mono = Geist_Mono({
  subsets: ['latin'],
  display: 'swap',
  variable: "--font-mono",
})
